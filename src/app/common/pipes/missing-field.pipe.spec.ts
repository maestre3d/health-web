import { MissingFieldPipe } from './missing-field.pipe';

describe('MissingFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new MissingFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
